name: Build Android APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # Boosted to the modern stable releases to match your Flet CLI demands
          channel: 'stable'

      - name: Build Android APK
        run: |
          # --no-interactive forces the compiler to say "yes" automatically to any download prompt
          flet build apk --no-interactive
          
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: scada-remote-app
          path: build/apk/*.apk
